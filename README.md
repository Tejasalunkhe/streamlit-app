# 🔐 Streamlit User Authentication App

A simple **Streamlit** application with user signup, login, and dashboard functionality.  
It uses **SQL Server (via pyodbc)** for database storage and **bcrypt** for secure password hashing.

---

## 🚀 Features
- User registration with strong password validation
- Secure login using hashed passwords
- Dashboard with session management
- Built with Streamlit for an interactive UI

---

## 🛠️ Requirements
Install dependencies from `requirements.txt`:

```bash
pip install -r requirements.txt
